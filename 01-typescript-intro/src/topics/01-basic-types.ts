
const name: string = 'Carlos';
let hpPoints: number | string = 95;
const isAlive: boolean = true;

hpPoints = 'FULL';

console.log({ name, hpPoints, isAlive });

export {}; // Evitar conflictos de nombres con otros archivos